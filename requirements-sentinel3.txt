# ============================================================================
# Ocean Sentinel V3.0 - Dépendances Ingestion Sentinel-3 (Mission 11)
# Optimisation Mémoire Stricte (< 256 Mo RAM)
# ============================================================================

# Base de données
psycopg2-binary==2.9.9

# Requêtes HTTP
requests==2.31.0

# Traitement données scientifiques NetCDF (OBLIGATOIRE pour lazy loading)
numpy==1.24.3
xarray==2023.1.0
netCDF4==1.6.3
dask==2023.1.0

# Logging et monitoring
python-dotenv==1.0.0

# Monitoring mémoire (inclus dans Python 3.4+)
# tracemalloc (built-in)
# gc (built-in)
